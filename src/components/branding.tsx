import H1 from "./h1";

export default function Branding() {
    return (
        <section>
            <H1>
                t<span className="font-semibold">system</span>
            </H1>
            <p className="text-lg opacity-80">
                A order management system built for trading.  
            </p>
        </section>
    );
}
